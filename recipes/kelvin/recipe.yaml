context:
  version: 0.3.1

package:
  name: "kelvin"
  version: ${{ version }}

source:
 - git: https://github.com/bgreni/Kelvin.git
   tag: 0.3.1

build:
  number: 0
  script:
    - mojo precompile kelvin -o ${{ PREFIX }}/lib/mojo/kelvin.mojoc

requirements:
  host:
    - mojo-compiler =1.1.0
  build:
    - mojo-compiler =1.1.0
  run:
    - ${{ pin_compatible('mojo-compiler') }}

tests:
  - script:
    - if: unix
      then:
        - python run_test.py
        - python scripts/generate_tests.py && python scripts/run_reject_tests.py

    files:
      source:
        - kelvin/
        - test/
        - run_test.py
        - scripts/run_reject_tests.py
        - scripts/generate_tests.py
    requirements:
      build:
        - mojo =1.1.0
      run:
        - mojo =1.1.0

about:
  homepage: https://github.com/bgreni/Kelvin
  license: Apache-2.0
  license_file: LICENSE
  summary: A powerful dimensional analysis library written in Mojo
  repository: https://github.com/bgreni/Kelvin

extra:
  maintainers:
    - bgreni
  project_name:
    - Kelvin
